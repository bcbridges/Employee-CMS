const inquirer = require("inquirer");
const connection = require("../config/connection");
const { Department, Role, Employee } = require("../models");

var Questions = {
  allEmpl: async function () {},
};

module.exports = Questions;
